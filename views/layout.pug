doctype html
html
  head
    title Pokemart Inventory
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale=1')
    link(rel='stylesheet', href='/stylesheets/style.css')
  body 
    .main-container 
      .navBar-container
        .top-navbar
          a( href='/' class='button-link' ) HOME
          a( href='/items' class='button-link' ) ALL ITEMS
          a( href='/categories' class='button-link' ) ALL CATEGORIES
          a( href='/itemcounts' class='button-link' ) ALL ITEM STOCK
        .bottom-navbar
          a( href='/item/create' class='button-link' ) ADD NEW ITEM
          a( href='/category/create' class='button-link' ) ADD NEW CATEGORY
          a( href='/itemcount/category' class='button-link' ) ADD ITEM QUANTITY
      .content-container
        block content
